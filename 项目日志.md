# CPPReactor 项目日志

## 2026-04-21 12:20:44 

- 版本：0.1.0。
- 当前进度：
  - `Channel`：C++ 回调与 RAII 已完成。
  - `EventLoop`：任务队列、wakeup、分发主循环已具备。
  - `EpollDispatcher`：`epoll_wait` 事件分发链路已建立。
  - `IOThreadPool`：worker 创建、停止、join 主流程已具备。
  - `TcpConnection`：C++ 类骨架与读写回调入口已建立。
- 待完成内容：
  - `TcpServer` 接线与主/子线程模式分支收敛。
  - 连接关闭流程闭环（连接级删除路径）。
  - `EventLoop` 的 `DELETE/MODIFY` 任务模型收敛。
  - HTTP 解析链路的 C++ 适配闭环。
- 行进目标：先完成连接生命周期闭环，再统一服务端接线路径，随后推进 HTTP 迁移收口。
- 提交记录：首次记录，无历史版本差异。

## 2026-04-22 01:17:22

- 版本：0.2.0。
- 当前进度：
  - `TcpConnection`：已接入 `HttpRequest`/`HttpResponse` C++ 类型成员，关闭路径已调用 `EventLoop::destroyTask`。
  - `EventLoop`：已新增 `destroyTask(int fd)` 接口，任务与事件主循环持续可用。
  - `Buffer`：已补充 `find`、`retrieve`、`retrieveAll`、`getStringView` 等接口以支持上层解析流程。
  - `HttpRequest`：已形成 C++ 类入口与基础解析框架，旧 C 版本逻辑已保留在条件编译区。
  - `HttpResponse`：已形成 C++ 类结构与就绪检查流程，旧 C 版本逻辑已保留在条件编译区。
- 待完成内容：
  - `TcpServer` 接线与主/子线程模式分支进一步收敛。
  - `EventLoop` 删除路径与 `channelMap_` 生命周期管理进一步闭环。
  - HTTP 请求行/请求头解析细节与响应组包流程继续收口并联调。
  - 入口与调用链的 C/C++ 混用路径继续统一。
- 行进目标：下一阶段聚焦 HTTP 链路可用性闭环与服务端接线一致性，保持版本在 `1.0.0` 以下推进。
- 提交记录：对比上一条记录，新增 `HttpRequest`/`HttpResponse` C++ 化骨架、`Buffer` 解析支撑接口及 `destroyTask` 删除接口。

## 2026-04-22 18:27:24

- 版本：0.3.0。
- 当前进度：
  - `CoreStatus`：新增统一状态码定义，事件循环与分发器返回值统一到 `StatusCode`。
  - `Dispatcher`：新增分发器工厂与类型映射（`epoll/poll/select`），支持按类型创建后端。
  - `EventLoop`：新增 `addTask(int fd, ChannelOP)` 路径，任务队列改为局部队列批处理，`socketpair` 唤醒改为非阻塞读写并补充错误事件分支。
  - `Epoll/Poll/SelectDispatcher`：分发返回值统一，`EINTR` 处理补齐，错误事件映射到 `kErrorEvent`。
  - `TcpServer/IOThreadPool`：支持按分发器类型创建 worker loop，补充 `start/stop` 防重复处理，`accept` 失败与空 loop 场景增加保护路径。
  - `TcpConnection`：读路径接入 `HttpRequest::parseRequest`，写路径默认响应联通，关闭路径统一投递 `DELETE` 任务。
  - `Buffer/HttpRequest/HttpResponse`：查找与切片边界处理完善，请求行/请求头解析流程收敛，就绪位清理逻辑修正。
- 待完成内容：
  - `main.cpp` 与 C++ 服务器入口链路的统一切换。
  - `TcpServer` 主/子线程模式分支继续收敛。
  - `TcpConnection` 与连接容器回收策略闭环（连接生命周期最终收口）。
  - HTTP 响应组包与文件发送路径接入新 C++ 响应对象闭环。
- 行进目标：先完成运行入口统一与连接回收闭环，再推进 HTTP 响应链路全流程打通。
- 提交记录：对比上一条记录，新增状态码体系、分发器工厂、多后端可选能力与事件错误路径处理，连接与请求解析链路继续向 C++ 主路径收敛。

## 2026-04-23 22:51:32

- 版本：0.4.0。
- 当前进度：
  - `main.cpp`：启动入口已切换为 C++ 服务器路径，支持 `dispatcher` 与线程数参数，日志输出接入 `spdlog`。
  - `TcpServer/IOThreadPool`：构造链路已透传 `DispatcherType`，worker loop 后端可选能力已落地。
  - `Metrics`：新增可观测性模块（`Metrics.hpp/.cpp`），已提供连接、请求、响应、字节与时延统计接口。
  - `Buffer`：`find/getStringView/retrieveAsString` 等接口边界处理继续完善。
  - `HttpRequest`：解析入口重构为 `parseRequest` 静态流程，解析状态失败路径已细分。
  - `HttpResponse`：就绪位判定与清理逻辑继续收敛，异常类型已调整为参数非法异常。
  - `scripts`：新增 `bench_http.sh` 压测脚本，支持 `wrk/ab/curl` 三种执行路径。
  - `第三方依赖`：新增 `spdlog/` 目录并纳入当前工程目录。
- 待完成内容：
  - `TcpServer::run` 调用链与返回状态在全链路的收口统一。
  - HTTP 响应对象到发送链路的完整落地与联调闭环。
  - 连接生命周期回收与容器管理策略的最终一致性收口。
  - 构建产物与源码目录边界（`.o`、可执行文件）整理。
- 行进目标：先完成运行链路可编译可启动的统一闭环，再推进 HTTP 响应与可观测性数据接线。
- 提交记录：对比上一条记录，新增 `Metrics` 模块、`bench_http.sh` 脚本、`spdlog` 依赖与 C++ 启动入口参数化，项目推进到多后端可选与基础可观测并行阶段。

## 2026-04-24 23:12:20

- 版本：0.5.0。
- 当前进度：
  - `TcpConnection`：补齐 Keep-Alive 连接策略（开关、最大请求数、空闲超时）与请求超时处理，读写路径已接入指标打点与基础响应回写。
  - `TcpServer`：接入连接关闭回调队列与定期清理流程，`accept` 循环加入非阻塞空转控制与周期指标日志输出。
  - `main.cpp`：启动参数扩展到连接模式与 Keep-Alive 策略，入口参数化完成度提升。
  - `HttpRequest`：解析流程支持增量解析与 `inFlight` 续解析，`Content-Length` 与请求体上限校验已落地。
  - `EventLoop`：同线程回调删除场景增加二次存在性检查，任务处理时序稳定性继续增强。
  - `HttpResponse`：头部存储结构改为键值对，准备态判定维持一致。
- 待完成内容：
  - HTTP 响应对象与真实文件/正文发送链路完整接线。
  - `TcpServer` 与连接容器回收策略的最终一致性收口。
  - Keep-Alive 模式下的边界场景回归验证与压测结果沉淀。
  - 构建产物与源码目录边界持续整理。
- 行进目标：先完成 Keep-Alive 与请求解析链路的稳定闭环，再推进响应发送与连接回收全路径收口。
- 提交记录：对比上一条记录，新增 Keep-Alive 与请求超时控制、增量请求解析、连接回收队列与周期指标日志，运行链路从“可启动”推进到“可持续连接管理”阶段。

## 2026-04-25 14:04:09

- 版本：0.6.0。
- 当前进度：
  - 目录结构：工程已完成分层重排，核心代码从根目录迁移到 `include/` 与 `src/`，并按 `core/net/protocol/observability/adapter/app` 分域组织。
  - 构建体系：新增 `CMakeLists.txt`，构建产物统一落到 `build/`，当前仍保留 `Makefile` 路径。
  - 依赖管理：`spdlog` 已迁移到 `third_party/spdlog/`，第三方目录边界明确。
  - 工程文档：`docs/` 迭代文档与阶段总结持续补齐，测试与基准文档落在 `tests/` 体系。
  - 脚本与测试：新增/保留 `scripts/bench_server_profiles.sh`、`scripts/test_smoke.sh` 与 `tests/run_http_cases.py`。
  - 全量核对：已检查项目目录与忽略名单，`build/` 下 CMake/obj 产物、`tests/bench_reports` 与 `tests/benchmarks` 本地基准文件处于忽略管理。
- 待完成内容：
  - 根目录旧路径文件删除后的引用与构建入口一致性最终收口。
  - 新目录结构下的编译、运行、压测链路持续联调。
  - 忽略名单与产物目录边界持续校准，避免运行产物进入源码域。
  - 迁移阶段版本继续保持在 `1.0.0` 以下递进。
- 行进目标：以新目录结构为主线完成构建与运行闭环稳定化，再推进协议链路与连接生命周期收口。
- 提交记录：对比上一条记录，本次重点为项目目录重构与构建/依赖布局升级，并完成包含忽略名单在内的全目录状态核对。

## 2026-05-03 23:42:45

- 版本：0.7.0。
- 当前进度：
  - 全量遍历：已完成项目目录遍历，当前结构稳定在 `include/`、`src/`、`scripts/`、`docs/`、`tests/`、`third_party/` 分层。
  - 构建链路：`CMakeLists.txt` 已接入 `spdlog`、`nlohmann_json` 与 MySQL Connector 依赖探测，`conanfile.py` 已纳入基础依赖管理。
  - SQL 模块：已新增 `adapter/sql` 目录与 `SqlConfig` 配置模型，包含驱动/连接模式枚举、字段校验与 JSON 反序列化流程。
  - SQL 基础设施：`JsonConfigLoader` 已新增并可复用到配置加载链路。
  - SQL 执行链路：`SqlRepository`、`ThreadLocalSqlConn` 文件已建立但实现仍为空，模块仍在接线阶段。
  - 运行入口：`src/app/main.cpp` 维持现有参数化启动路径与静态文件适配器挂载。
- 待完成内容：
  - `SqlRepository` 查询执行与结果映射实现落地。
  - `ThreadLocalSqlConn` 连接生命周期与线程局部复用策略落地。
  - SQL 配置文件接入主启动链路并完成加载联调。
  - SQL 模块与现有请求处理链路的调用接线收口。
- 行进目标：先完成 SQL 模块核心执行链路闭环，再推进与服务主流程的联调稳定化。
- 提交记录：对比上一条记录，本次新增重点为 SQL 配置与目录骨架落地，并完成全项目遍历后的阶段进度更新。

## 2026-05-13 00:30:00

- 版本：0.8.0。
- 当前进度：
  - 目录重组：`adapter/` 层已拆分为三个独立层：`handler/`（请求处理器）、`persistence/`（数据持久化）、`repository/`（数据仓库），分层边界更清晰。
  - 命名空间重构：命名空间已根据目录层级重新映射——`reactor::handler`、`reactor::persistence`、`reactor::repository::sql`，目录结构与命名空间严格对应。
  - 类名重命名：`StaticFileAdapter` → `StaticFileHandler`，归属 `reactor::handler` 命名空间。
  - SQL 组件迁移：`adapter/sql/*` 整体迁移至 `persistence/`（接口/驱动/执行器）与 `repository/sql/`（仓储/任务模型），组件能力完整但保持未接入状态。
  - 新增空桩文件：`IHttpHandler.hpp`、`SqlHandler.hpp`、`HttpRouter.hpp` 已创建，为后续请求路由与 SQL 接入预留接口位置。
  - 旧目录清理：`include/adapter/` 与 `src/adapter/` 已完整删除，无残留引用。
  - 构建修复：`Makefile` 已补充 `nlohmann_json`（conan 缓存）及 `mysql-cppconn`（X DevAPI 库 `libmysqlcppconnx`）的 include/link 路径。
- 验证结果：
  - 编译通过：`make clean && make -j$(nproc)` 零错误零警告完成。
  - 冒烟测试：`curl` 请求 `/`（目录索引 200）、`/index.html`（200）、`/healthz`（200）均正常响应。
  - 压测数据（close 模式，epoll，2 线程）：
    - `/`（目录页）：~8745 req/s，avg latency 1.11ms，50% ≤ 1.11ms，99% ≤ 1.36ms
    - `/healthz`：~8382 req/s，avg latency 1.11ms，50% ≤ 1.11ms，99% ≤ 1.30ms
  - 性能无退化：压测数据与重构前持平，目录重组未引入性能损失。
- 待完成内容：
  - `IHttpHandler` / `HttpRouter` / `SqlHandler` 三个桩文件的接口定义与实现落地。
  - SQL 模块与请求处理链路的异步化接线（每个 Reactor 对应一个 SqlExecutor）。
  - `EventLoop` 的 `post()` 回调投递机制实现。
  - 构建体系的 CMakeLists.txt 与 Makefile 双路径持续维护。
- 行进目标：先保持 0.8.0 版本下的构建与运行稳定，再推进请求路由与 SQL 异步化接入闭环。
- 提交记录：对比上一条记录，本次重点为 adapter 层拆分为 handler/persistence/repository 三层、命名空间规范化、Makefile 依赖修复，以及编译/运行/压测全链路验证通过。

## 2026-05-19 1:17:00

- 版本：0.9.0。
- 当前进度：
  - `EventLoop::post()` 跨线程回调机制：新增 `callbackQueue_` 队列与 `post(Callback)` 方法，跨线程投递时通过 `socketpair` 唤醒目标 EventLoop，`run()` 每轮末尾批量执行回调队列。
  - 异步响应支持：`TcpConnection` 继承 `enable_shared_from_this`，`create()` 返回 `shared_ptr`；`HandlerResult` 新增 `async` 标记；处理器可返回异步标记跳过同步写路径，通过 `sendAsyncResponse()` 跨线程回写结果。
  - 连接生命周期 `shared_ptr` 化：`TcpServer::conns_` 从 `unique_ptr` 改为 `shared_ptr`，`TcpServer.hpp` 对应调整。
  - `SqlHandler` 完整实现：`SqlHandler::createHandler(sqlExecutor)` 返回 `RequestHandler`，通过 URL `?sql=` 参数提取 SQL，提交到 `SqlExecutor` 后台线程执行，结果通过 `EventLoop::post()` 投递回主线程，以 JSON 格式异步回写 HTTP 响应。
  - `StringUtils` 公共工具模块：`hexValue()`、`urlDecode()`、`urlEncodePathComponent()` 从 `StaticFileHandler.cpp` 内联实现提取到 `include/utils/StringUtils.hpp`。
  - `StaticFileHandler` 适配：移除内联 URL 编解码函数，改用 `utils::StringUtils`；`RequestHandler` 适配新签名 `(HttpRequest&, TcpConnection&)`。
  - `.clangd` 独立配置：从 CMake CompilationDatabase 依赖切换为手动指定 `-std=c++20` 与头文件搜索路径。
  - `config/SQLConfig.json`：SQL 连接配置文件，包含连接参数、超时规则、连接模式、观测与保护配置。
- 待完成内容：
  - `SqlHandler` 接入 `main.cpp` 启动链路并与静态文件适配器共存验证。
  - HTTP 路由分发（`HttpRouter`）实现，支持按路径匹配不同处理器。
  - 构建体系 CMakeLists.txt 与 Makefile 双路径同步维护。
  - 压测验证异步 SQL 链路下无连接泄漏与性能退化。
- 行进目标：先完成 SqlHandler 与 main 链路的接入联调，再推进 HTTP 路由分发使多处理器可共存。
- 提交记录：对比上一条记录，本次实现了 EventLoop 跨线程回调投递、TcpConnection 异步响应机制、SqlHandler 完整 SQL 查询处理器，以及工具函数模块化提取。

## 2026-05-19 13:17:00

- 版本：0.10.0。
- 当前进度：
  - `main.cpp` SQL 全链路接线：加载 `config/SQLConfig.json` → 创建 `SqlExecutor`（`ThreadLocalSingleConn` + `MySqlConnection` 工厂）→ 启动执行器 → 注册复合请求处理器（`/query`/`/sql` → SqlHandler，其余 → StaticFileHandler）
  - `scripts/seed_data.py`：新增 Python 数据注入脚本，支持读取 `SQLConfig.json` 或命令行参数，向 `users` 表插入默认 50,000 行测试数据，批处理含进度显示
- 待完成内容：
  - `HttpRouter` 独立路由组件实现，替代当前 lambda 路由闭包
  - 构建体系 CMakeLists.txt 与 Makefile 双路径同步维护
  - 编译/冒烟/压测全链路验证（当前暂未编译验证）
  - SQL 慢查询日志与连接池度量埋点
- 行进目标：先完成全链路编译验证后提交，再推进 HttpRouter 独立化与可观测性补全。
- 提交记录：对比上一条记录，本轮将 SQL 从独立模块推进到主入口接线阶段，并补充了测试数据注入脚本。

## 2026-06-02 01:12:00

- 版本：0.15.0（SQL 集成阶段完结）。
- 当前进度：
  - `MySqlConnection` JDBC 重构：从 X DevAPI（`mysqlx::Session`）切换为 JDBC Connector/C++（`sql::Driver` + `ConnectOptionsMap`），全面兼容 MySQL 5.7，支持 `OPT_RECONNECT`、超时、字符集等全部连接参数
  - 全链路闭环验证：`main.cpp` 加载 `SQLConfig.json` → 创建 `SqlExecutor` → 复合路由（`/query`/`/sql` → SqlHandler，其余 → StaticFileHandler），编译通过，远程数据库查询可用
  - 压测体系完善：
    - `tests/bench_sql.sh`：SQL 压测脚本，S/M/L/ALL 四档，自动端口检测 + 服务器启停 + Markdown 输出
    - `tests/BENCHMARK_REPORT.md`：基准压测报告（静态最高 QPS 10,143；SQL 远程查询 QPS ~15，瓶颈在远端单核 MySQL）
    - `tests/benchmarks/`：压测输出保留为可追踪文档
  - 测试基础设施完备：
    - `tests/setup.sh` + `tests/requirements.txt`：Python 虚拟环境一键初始化
    - `tests/seed_data.py`、`tests/test_smoke.sh`：从 `scripts/` 迁移至 `tests/`
    - `tests/README.md`：完整文档覆盖
  - 构建适配：`Makefile` 链接库 `-lmysqlcppconnx` → `-lmysqlcppconn`
  - `SqlExecutor` 异常捕获增强：`mysqlx::Error` → `std::exception` + `spdlog::warn`
  - `.gitignore` 全面更新，忽略测试产物与虚拟环境
- 待完成内容（后续阶段）：
  - `HttpRouter` 独立路由组件实现
  - `IHttpHandler` 请求处理器接口实现
  - `SqlRepository::findUserByIdAsync` 回调接线
  - CMakeLists.txt 与 Makefile 双路径同步维护
- 行进目标：本阶段已达预期——SQL 异步查询链路完整交付，测试与压测体系可用。下一阶段可推进 HttpRouter 路由分发与仓储层回调闭环。
- 提交记录：对比上一条记录，本轮完成了 3 次提交：
  1. `7c6f8ae` — SQL 回调与异步链路基本完成
  2. `ef9263b` — SQL 模块并入主链路
  3. `55117e2` — 完整压测和测试压力
  核心变化：JDBC 驱动重构、全链路接线、压测体系与测试基础设施完整交付。
